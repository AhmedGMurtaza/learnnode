extends layout

block content
    h2= article.title
    h5= "written by: "+ article.author
    p=article.body 
    a.btn.btn-success(href='/articles/edit/'+article._id) Edit
    a.btn.btn-danger.delete-article(href='/articles/'+article._id,data-id=article._id) Delete


